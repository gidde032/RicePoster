fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9
anthropic==0.34.0
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.0
aiofiles==24.1.0
playwright==1.49.0

# Development / quality gates. Not imported by backend/, but the test suite
# and both pre-commit hooks are unrunnable without them, so a clone that
# installs only the runtime deps cannot commit (audit Phase 2, finding #5).
pytest==9.0.3
pytest-cov==7.1.0
pre-commit==4.6.0
